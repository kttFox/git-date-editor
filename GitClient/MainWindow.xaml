<Window x:Class="GitClient.MainWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:local="clr-namespace:GitClient"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		Title="{Binding Title}"
		Width="800"
		Height="450"
		mc:Ignorable="d">
	<Window.DataContext>
		<local:MainWindowViewModel />
	</Window.DataContext>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="1*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="Auto" />
			</Grid.RowDefinitions>

			<Grid Height="25">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="1*" />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>
				<TextBox VerticalContentAlignment="Center"
						 Text="{Binding Path}" />
				<Button Grid.Column="1"
						Width="60"
						Command="{Binding ApplyCommand}"
						Content="取得" />
			</Grid>
		</Grid>

		<DataGrid Grid.Row="1"
				  AutoGenerateColumns="False"
				  HorizontalGridLinesBrush="LightGray"
				  IsReadOnly="True"
				  ItemsSource="{Binding Items}"
				  KeyDown="DataGrid_KeyDown"
				  VerticalGridLinesBrush="LightGray">
			<DataGrid.Columns>
				<DataGridCheckBoxColumn Width="50"
										Binding="{Binding IsSelected}"
										Header="選択"
										IsReadOnly="False">
					<DataGridCheckBoxColumn.ElementStyle>
						<Style TargetType="CheckBox">
							<Setter Property="HorizontalAlignment" Value="Center" />
							<Setter Property="VerticalAlignment" Value="Center" />
						</Style>
					</DataGridCheckBoxColumn.ElementStyle>
				</DataGridCheckBoxColumn>
				<DataGridTextColumn Width="100"
									Binding="{Binding Commit.Sha,
													  StringFormat=0:G8}"
									Header="SHA">
					<DataGridTextColumn.ElementStyle>
						<Style TargetType="TextBlock">
							<Setter Property="Margin" Value="5,0" />
							<Setter Property="VerticalAlignment" Value="Center" />
							<Setter Property="TextTrimming" Value="CharacterEllipsis" />
						</Style>
					</DataGridTextColumn.ElementStyle>
				</DataGridTextColumn>
				<DataGridTextColumn Width="*"
									Binding="{Binding Commit.MessageShort}"
									Header="メッセージ">
					<DataGridTextColumn.ElementStyle>
						<Style TargetType="TextBlock">
							<Setter Property="Margin" Value="5,0" />
							<Setter Property="VerticalAlignment" Value="Center" />
						</Style>
					</DataGridTextColumn.ElementStyle>
				</DataGridTextColumn>
				<DataGridTemplateColumn Width="100"
										Header="作者">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<TextBlock Margin="5,0"
									   VerticalAlignment="Center"
									   Text="{Binding Commit.Author,
													  StringFormat={}{0:Name} &lt;{0:Email}&gt;}" />
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
				<DataGridTextColumn Width="150"
									Binding="{Binding Commit.Committer.When,
													  StringFormat=yyyy/M/d H:mm}"
									Header="変更日">
					<DataGridTextColumn.ElementStyle>
						<Style TargetType="TextBlock">
							<Setter Property="Margin" Value="5,0" />
							<Setter Property="VerticalAlignment" Value="Center" />
						</Style>
					</DataGridTextColumn.ElementStyle>
				</DataGridTextColumn>
				<DataGridTextColumn Width="150"
									Binding="{Binding Commit.Author.When,
													  StringFormat=yyyy/M/d H:mm}"
									Header="作成日">
					<DataGridTextColumn.ElementStyle>
						<Style TargetType="TextBlock">
							<Setter Property="Margin" Value="5,0" />
							<Setter Property="VerticalAlignment" Value="Center" />
						</Style>
					</DataGridTextColumn.ElementStyle>
				</DataGridTextColumn>
			</DataGrid.Columns>
		</DataGrid>

		<Grid Grid.Row="2"
			  Height="25">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>

			<TextBox Width="200"
					 VerticalContentAlignment="Center"
					 Text="{Binding NewDateTime}" />
			<Button Grid.Column="1"
					Width="100"
					Command="{Binding RunCommand}"
					Content="日付変更" />
		</Grid>
	</Grid>
</Window>
